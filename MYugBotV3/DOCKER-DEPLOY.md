# 🚀 Быстрое развертывание MYugBotV3 в Docker

## 📋 Предварительные требования

На целевом ПК должны быть установлены:
- Docker (версия 20.10+)
- Docker Compose (версия 2.0+)
- Доступ к базе данных Firebird ITM

## ⚡ Быстрый старт (3 шага)

### 1️⃣ Подготовка на исходном ПК

```bash
cd /Users/mironocean/Documents/Progs/MYugBotV3

# Создать архив проекта
tar -czf myugbot-v3.tar.gz \
  --exclude=node_modules \
  --exclude=dist \
  --exclude=ITM.FDB \
  --exclude=.git \
  .
```

### 2️⃣ Копирование на целевой ПК

```bash
# Скопировать архив на целевой ПК (замените на ваш IP/путь)
scp myugbot-v3.tar.gz user@target-pc:/path/to/destination/

# Или используйте USB/сетевую папку
```

### 3️⃣ Развертывание на целевом ПК

```bash
# Распаковать архив
cd /path/to/destination
tar -xzf myugbot-v3.tar.gz
cd MYugBotV3

# Настроить переменные окружения
cp .env.production .env
nano .env  # Отредактировать параметры (см. ниже)

# Запустить через Docker Compose
docker-compose up -d
```

## 🔧 Настройка переменных окружения (.env)

Отредактируйте файл `.env` со следующими параметрами:

```env
# Токен вашего Telegram бота (получите у @BotFather)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# База данных Firebird
DB_HOST=192.168.1.100        # IP сервера БД или localhost
DB_PORT=3050                 # Порт Firebird (обычно 3050)
DB_NAME=/firebird/data/ITM.FDB   # Путь к файлу БД
DB_USER=SYSDBA              # Пользователь БД
DB_PASSWORD=your_password   # Пароль БД
```

### 📍 Примеры конфигурации БД

**Вариант 1: БД на том же ПК**
```env
DB_HOST=localhost
DB_NAME=/var/firebird/ITM.FDB
```

**Вариант 2: БД на удаленном сервере**
```env
DB_HOST=192.168.1.50
DB_NAME=ITM.FDB
```

**Вариант 3: БД через Docker volume**
```env
DB_HOST=localhost
DB_NAME=/firebird/data/ITM.FDB
```

## 🐳 Команды Docker

### Запуск бота
```bash
# Первый запуск (сборка + запуск)
docker-compose up -d --build

# Последующие запуски
docker-compose up -d
```

### Просмотр логов
```bash
# Показать последние логи
docker-compose logs

# Следить за логами в реальном времени
docker-compose logs -f

# Логи за последние 50 строк
docker-compose logs --tail=50
```

### Остановка и перезапуск
```bash
# Остановить бота
docker-compose down

# Перезапустить бота
docker-compose restart

# Перезапустить с пересборкой
docker-compose down
docker-compose up -d --build
```

### Управление контейнером
```bash
# Проверить статус
docker-compose ps

# Выполнить команду внутри контейнера
docker-compose exec telegram-bot sh

# Удалить контейнер и образ
docker-compose down --rmi all
```

## 📦 Структура проекта

```
MYugBotV3/
├── Dockerfile              # Многоэтапная сборка Docker образа
├── docker-compose.yml      # Конфигурация Docker Compose
├── .dockerignore          # Исключения для Docker
├── .env                   # Переменные окружения (не в git)
├── .env.production        # Шаблон для production
├── scripts/
│   └── migrate.js         # Скрипт миграции БД
├── src/                   # Исходный код
├── package.json           # Зависимости Node.js
└── tsconfig.json          # Конфигурация TypeScript
```

## 🔄 Процесс развертывания

При запуске контейнера автоматически выполняются:

1. **Миграция БД** (`scripts/migrate.js`):
   - Создание таблицы `TG_USERS`
   - Создание генератора и триггера для автоинкремента
   - Создание индексов
   - Создание хранимой процедуры `TGP_CREATE_USER`
   - ✅ Безопасно: пропускает существующие объекты

2. **Запуск бота**:
   - Подключение к базе данных
   - Инициализация Telegram бота
   - Готовность к приему команд

## 🛠️ Ручная миграция БД (опционально)

Если нужно запустить миграцию отдельно:

```bash
# Вне Docker (на хосте)
npm install
node scripts/migrate.js

# Внутри Docker контейнера
docker-compose exec telegram-bot node scripts/migrate.js
```

## 🌐 Настройка сети

### Если БД на localhost
Раскомментируйте в `docker-compose.yml`:
```yaml
network_mode: "host"
```

### Если БД через volume
Добавьте в `docker-compose.yml`:
```yaml
volumes:
  - /path/to/firebird/data:/firebird/data:ro
```

## 📊 Проверка работы

### 1. Проверить логи запуска
```bash
docker-compose logs
```

Должны увидеть:
```
✅ Пул соединений с ITM DB создан
✅ Telegram бот запущен и готов к работе
```

### 2. Проверить статус контейнера
```bash
docker-compose ps
```

Статус должен быть `Up`.

### 3. Проверить миграцию БД
Логи должны показать:
```
🔧 Database Migration for MYugBotV3
✅ Connection established
[1/6] Create TG_USERS table...
  ✅ Success
...
📊 Migration Results:
  ✅ Successful: 6
```

### 4. Протестировать бота
- Откройте Telegram
- Найдите вашего бота
- Отправьте `/start`
- Проверьте ответ бота

## 🔍 Отладка проблем

### Бот не подключается к БД

**Проблема:** `Connection error: Error: connect ECONNREFUSED`

**Решение:**
1. Проверьте `DB_HOST` и `DB_PORT` в `.env`
2. Убедитесь, что Firebird сервер запущен
3. Проверьте firewall
4. Используйте `network_mode: "host"` если БД на localhost

### Бот не отвечает в Telegram

**Проблема:** Бот онлайн, но не отвечает

**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
2. Убедитесь, что токен правильный
3. Проверьте логи: `docker-compose logs -f`

### Ошибки миграции БД

**Проблема:** `Already exists` или `duplicate`

**Решение:**
- Это нормально! Миграция пропускает существующие объекты
- Если ошибка другая - проверьте права доступа к БД

### Контейнер постоянно перезапускается

**Проблема:** `Status: Restarting`

**Решение:**
1. Проверьте логи: `docker-compose logs --tail=100`
2. Проверьте `.env` на опечатки
3. Убедитесь, что БД доступна

## 🔄 Обновление бота

```bash
# Остановить бота
docker-compose down

# Обновить код (git pull или скопировать новые файлы)
# ...

# Пересобрать и запустить
docker-compose up -d --build
```

## 🗄️ Резервное копирование

### Экспорт конфигурации
```bash
# Сохранить .env и docker-compose.yml
cp .env .env.backup
cp docker-compose.yml docker-compose.backup.yml
```

### Экспорт данных БД
```bash
# Создать бэкап Firebird БД
gbak -b -user SYSDBA -password masterkey \
  /path/to/ITM.FDB /path/to/backup/ITM_backup.fbk
```

## 📝 Полезные команды

```bash
# Посмотреть использование ресурсов
docker stats myug-bot-v3

# Очистить логи
docker-compose logs --no-log-prefix > /dev/null

# Перезапустить только миграцию
docker-compose exec telegram-bot node scripts/migrate.js

# Войти в контейнер для отладки
docker-compose exec telegram-bot sh

# Проверить переменные окружения
docker-compose exec telegram-bot env | grep DB_
```

## ⚙️ Production настройки

### 1. Автозапуск при перезагрузке системы
Уже настроено в `docker-compose.yml`:
```yaml
restart: unless-stopped
```

### 2. Ротация логов
Уже настроена в `docker-compose.yml`:
```yaml
logging:
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"
```

### 3. Мониторинг (опционально)
```bash
# Установить healthcheck
# Добавьте в docker-compose.yml:
healthcheck:
  test: ["CMD", "node", "-e", "process.exit(0)"]
  interval: 30s
  timeout: 10s
  retries: 3
```

## 🚨 Безопасность

1. **Не сохраняйте .env в git**
   - Уже добавлено в `.gitignore`

2. **Используйте сильные пароли БД**
   - Измените пароль по умолчанию

3. **Ограничьте доступ к БД**
   - Настройте firewall для порта 3050

4. **Регулярные бэкапы**
   - Настройте автоматическое резервное копирование БД

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус: `docker-compose ps`
3. Проверьте подключение к БД: `docker-compose exec telegram-bot node scripts/migrate.js`
4. Проверьте `.env` файл на корректность

---

## ✅ Чеклист развертывания

- [ ] Docker и Docker Compose установлены
- [ ] Firebird БД доступна и запущена
- [ ] Токен бота получен от @BotFather
- [ ] Файл `.env` создан и заполнен
- [ ] Путь к БД в `.env` правильный
- [ ] `docker-compose up -d --build` выполнен
- [ ] Логи показывают успешный запуск
- [ ] Миграция БД выполнена успешно
- [ ] Бот отвечает на `/start` в Telegram

---

**Время развертывания: ~5-10 минут** ⚡

**Готово! Ваш бот работает в Docker! 🎉**
