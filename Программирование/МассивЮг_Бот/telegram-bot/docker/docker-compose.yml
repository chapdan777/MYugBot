version: '3.8'
services:
  bot:
    build: .
    container_name: telegram-bot
    env_file:
      - ../.env
    restart: unless-stopped
    networks:
      - bot-net
networks:
  bot-net:
    driver: bridge
